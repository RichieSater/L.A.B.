# Lint And Harness Alignment

## Goal

Bring the repo back to a clean baseline by fixing current lint failures and updating the harness notes so they describe the real product shape instead of the default Vite template.

## Plan

1. Refresh and validate the harness.
2. Fix the current ESLint failures without changing product behavior.
3. Add durable notes about the client, API, auth, and persistence boundaries.
4. Re-run lint, tests, and build.
